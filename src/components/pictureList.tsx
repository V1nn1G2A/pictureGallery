import React from 'react';
import PictureItem from '../UI/pictureItem/pictureItem';

const pictureList = () => {
  return (
    <ul>
      <PictureItem />
    </ul>
  );
};

export default pictureList;
